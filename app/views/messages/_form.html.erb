
<%= form_for(@message) do |f| %>


  <div class="field">
    <p>Enter your message, hashtags will be automatically parsed if separated by spaces</p><br>
    <%= f.text_area :message, cols: 100, rows: 10 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
